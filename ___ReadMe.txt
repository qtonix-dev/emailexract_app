+++ EXTRA +++
-> User section add
-> User module



---Features---
-> Faster Page Change / Data Loading
   Page Change Time -> 0.001 to 0.010 second. (avg 0.002 second)
   Data Loading Time -> 1.000 to 3.000 second. (avg 2 second)

-> Advance Bulk Search System
    - Speed regulation
      (Admin can manage speed)

    - Set upload and extract limit
      (Amin can set/update max upload and extract limit)

    - Manual / Auto Data Save
      (User can save his data maually while extracting, data will be save automatically after extraction)

    - Advanced Page Detection
      (If user change page while extraction then data will be saved automatically, and user can download any time)

    - Advanced custom CAPTCHA algorithm
      (Custom AlphaNumeric CAPTCHA we can modify and redesign our CAPTCHA according our needs)

    - Advanced custom user bulk extracting detection
      (Our algorithm will detect multiple tab, multiple login, multiple extraction, data will be auto save if our algorithm detect something in user account.)

-> Custom activity tracking algorithm
   (algorithm will track mousemove, mousedown, keydown, touchstart, scroll)
   (User account will be auto logout after 30min if no activity detected)